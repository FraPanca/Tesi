# SOLO PER ESECUZIONI LOCALI (npm run dev, fuori Docker).
# Il container backend avviato con docker compose NON legge questo file:
# riceve le variabili da docker-compose.yml, valorizzato da .env nella root del repo.
#
# TEMPLATE — copia questo file in ".env" e inserisci i valori reali.
# cp .env.example .env

###############
#### INFRA ####
###############

PORT=3000
MONGO_URI=mongodb://localhost:27017/iot_energy
REDIS_URI=redis://localhost:6379
MQTT_BROKER_URL=mqtt://localhost:1883


##############
#### MQTT ####
##############

MQTT_USER=
MQTT_PASSWORD=


#############
#### JWT ####
#############

JWT_SECRET=una-stringa-lunga-e-casuale  # Genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_EXPIRES_IN=12h


###############
#### ADMIN ####
###############

ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=    # Genera con: node -e "require('bcryptjs').hash(process.argv[1], 10).then(console.log)" 'la-tua-password'